const section = {
    id: 97,
    title: "What is an Algorithm ?",
    content:
    `<div id="body-content" class="fs-5">
    <style>.layout_size{min-height: 118px;overflow: hidden;margin-bottom:10px;} @media (max-width: 550px) { .layout_size {min-height: 320px; min-width: inherit; } }
    table, td, th {
        border: 1px solid black;
      }
      
      table {
        width: 100%;
        border-collapse: collapse;
      }
      img {
          margin: 0 auto;
      }
      ol{ 
          padding: 0 40px;
      }
      font-size: 20px;
    </style>   
    <p>An algorithm is a finite set of instructions or logic, written in order, to accomplish a certain predefined task. Algorithm is not the complete code or program, it is just the core logic(solution) of a problem, which can be expressed either as an informal high level description as <b>pseudocode</b> or using a <b>flowchart</b>. </p>
    <p>Every Algorithm must satisfy the following properties:</p>
    <ol class="content">
    <li><b>Input</b>- There should be 0 or more inputs supplied externally to the algorithm.</li>
    <li><b>Output</b>- There should be atleast 1 output obtained.</li>
    <li><b>Definiteness</b>- Every step of the algorithm should be clear and well defined.</li>
    <li><b>Finiteness</b>- The algorithm should have finite number of steps.</li>
    <li><b>Correctness</b>- Every step of the algorithm must generate a correct output.</li>
    </ol>
    <img src="https://media.geeksforgeeks.org/wp-content/cdn-uploads/20191016135223/What-is-Algorithm_-1024x631.jpg" alt="algorigms" />
    <p>An algorithm is said to be efficient and fast, if it takes less time to execute and consumes less memory space. The performance of an algorithm is measured on the basis of following properties :</p>
    <ol class="content">
    <li>Time Complexity</li>
    <li>Space Complexity</li>
    </ol>
    `
};
export default section;